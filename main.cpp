#include <iostream>
#include <tannic.hpp> 

/*
compile the library with ```bash buid.sh```
run this file with ```bash main.sh```
run the tests with ```bash test.sh```
*/

using namespace tannic;

int main() {
    std::cout << "Playground" << std::endl;
}
